#include <stdio.h>
/**
 * main - function that prints string
 * Return: returns 0 when the  program  execute successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
