#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int nbr;

	for (nbr = 48 ; nbr <= 57 ; nbr++)
		putchar(nbr);

	putchar('\n');

	return (0);
}
