#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int nbr;

	for (nbr = 0 ; nbr <= 9 ; nbr++)
		putchar(nbr);

	putchar('\n');

	return (0);
}
